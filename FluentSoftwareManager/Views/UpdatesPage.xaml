<Page
    x:Class="FluentSoftwareManager.Views.UpdatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:FluentSoftwareManager.Models"
    x:Name="PageRoot"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                      Text="Available Updates"
                      Style="{StaticResource TitleTextBlockStyle}"/>

            <Button Grid.Column="1"
                   Command="{x:Bind ViewModel.UpdateAllCommand}"
                   Margin="0,0,12,0"
                   Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE895;"/>
                    <TextBlock Text="Update All"/>
                </StackPanel>
            </Button>

            <Button Grid.Column="2"
                   Command="{x:Bind ViewModel.RefreshCommand}"
                   ToolTipService.ToolTip="Refresh">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;"/>
                    <TextBlock Text="Refresh"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Info Card -->
        <InfoBar Grid.Row="1"
                IsOpen="True"
                Severity="Informational"
                Message="Keep your software up to date for the latest features and security improvements"
                Margin="0,0,0,16"/>

        <!-- Package List -->
        <ListView Grid.Row="2"
                 ItemsSource="{x:Bind ViewModel.Packages, Mode=OneWay}"
                 SelectedItem="{x:Bind ViewModel.SelectedPackage, Mode=TwoWay}"
                 SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Package">
                    <Grid Padding="12" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Icon -->
                        <Border Grid.Column="0"
                               Width="48" Height="48"
                               Background="{ThemeResource AccentFillColorDefaultBrush}"
                               CornerRadius="8">
                            <FontIcon Glyph="&#xE896;" FontSize="24"/>
                        </Border>

                        <!-- Package Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}"
                                      FontWeight="SemiBold"
                                      FontSize="14"/>
                            <TextBlock Text="{x:Bind Id}"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Foreground="{ThemeResource TextFillColorTertiaryBrush}" FontSize="11">
                                    <Run Text="{x:Bind InstalledVersion}"/>
                                    <Run Text="→"/>
                                    <Run Text="{x:Bind AvailableVersion}" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <!-- Update Button -->
                        <Button Grid.Column="2"
                               Content="Update"
                               Command="{Binding DataContext.UpdatePackageCommand, ElementName=PageRoot}"
                               CommandParameter="{x:Bind}"
                               Style="{StaticResource AccentButtonStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Status Bar -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ProgressRing Grid.Column="0"
                         IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
                         Width="20" Height="20"
                         Margin="0,0,12,0"/>

            <TextBlock Grid.Column="1"
                      Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                      VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>
